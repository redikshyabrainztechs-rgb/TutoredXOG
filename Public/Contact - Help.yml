info:
  name: "Contact / Help "
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/public/create-help"
  body:
    type: json
    data: |-
      {
        "full_name":"REDIKSHYA PHUYAL",
       "email":"redikshya.brainztechs+y@gmail.com",
       "department":"student-support",
       "reason":"booking-issue",
       "subject":"cannot book a session",
       "message":"trying to book smirti bam's group session",
        
       "attachment":{}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
