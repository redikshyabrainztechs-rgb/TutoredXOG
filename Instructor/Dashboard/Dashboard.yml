info:
  name: Dashboard
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/users/notification"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
