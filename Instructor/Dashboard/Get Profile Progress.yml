info:
  name: Get Profile Progress
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/instructor/get-profile-progress"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
