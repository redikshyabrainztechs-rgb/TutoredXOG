info:
  name: Intructor Stats for Session
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/instructor/list/session-bookings"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
