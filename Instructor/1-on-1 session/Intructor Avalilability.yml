info:
  name: Intructor Avalilability
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/instructor/instructor-stats-for-session"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
