info:
  name: One on one toggle
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseURL}}/instructor/update-basic-session"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
