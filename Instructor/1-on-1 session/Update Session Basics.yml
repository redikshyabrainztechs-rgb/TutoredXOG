info:
  name: Update Session Basics
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseURL}}/instructor/availability"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
