info:
  name: "Get Instructor's Live Session Stats "
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/instructor/live-sessions/stats"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
