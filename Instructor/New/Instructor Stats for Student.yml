info:
  name: Instructor Stats for Student
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/instructor/instructor-stats-for-student"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
