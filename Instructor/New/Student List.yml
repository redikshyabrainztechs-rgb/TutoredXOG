info:
  name: Student List
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/instructor/list/students"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
