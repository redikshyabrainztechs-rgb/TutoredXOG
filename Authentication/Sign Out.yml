info:
  name: Sign Out
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseURL}/users/logout"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
