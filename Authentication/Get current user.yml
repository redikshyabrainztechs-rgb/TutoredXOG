info:
  name: Get current user
  type: http
  seq: 3

http:
  method: GET
  url: https://mw7wpcy6jtbduqe2jm63l4nm7i0brags.lambda-url.ap-south-1.on.aws/api/users/me
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
