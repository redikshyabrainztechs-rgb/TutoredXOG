info:
  name: Verify Email
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseURL}}/users/request-verify-email"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
