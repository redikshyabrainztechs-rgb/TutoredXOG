info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email" :"redikshya.brainztechs+y@gmail.com",
          "password" : "apple111@A"
      }
  auth: inherit

runtime:
  variables:
    - name: baseURL
      value: https://mw7wpcy6jtbduqe2jm63l4nm7i0brags.lambda-url.ap-south-1.on.aws/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
